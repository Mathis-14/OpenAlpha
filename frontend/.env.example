# Backend API URL (must be reachable from the browser). Leave unset to use http://127.0.0.1:8000
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Optional: server-side fetch URL (e.g. Docker: http://backend:8000). Browser still uses NEXT_PUBLIC_*.
# INTERNAL_API_URL=http://backend:8000
